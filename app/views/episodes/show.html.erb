<div class="bg-white shadow overflow-hidden sm:rounded-lg">
  <div class="px-4 py-5 sm:px-6">
    <h1 class="text-2xl font-bold text-gray-900"><%= @episode.title %></h1>
    <p class="mt-1 max-w-2xl text-sm text-gray-500">
      Published <%= time_ago_in_words(@episode.published_at) %> ago
    </p>
  </div>
  
  <div class="border-t border-gray-200 px-4 py-5 sm:px-6">
    <div class="prose prose-indigo max-w-none">
      <%= simple_format @episode.description, class: "text-gray-700" %>
    </div>

    <div class="mt-6">
      <div class="flex items-center text-sm text-gray-500">
        <svg class="flex-shrink-0 mr-1.5 h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd" />
        </svg>
        Duration: <%= pluralize(@episode.duration / 60, 'minute') %>
      </div>

      <div class="mt-4">
        <audio controls class="w-full">
          <source src="<%= @episode.audio_url %>" type="audio/mpeg">
          Your browser does not support the audio element.
        </audio>
      </div>
    </div>
  </div>
    
  <div class="px-4 sm:px-6">
    <%= form_with url: toggle_like_episode_path(@episode), method: :post, local: true do %>
      <button type="submit" style="border: none; background: none;">
        <% if @episode.likes.exists?(user: current_user) %>
          <%= image_tag "red-heart-icon.svg", alt: "Liked", class: "w-6 h-6" %>
        <% else %>
          <%= image_tag "heart-thin-icon.svg", alt: "Not Liked", class: "w-6 h-6" %>
        <% end %>
      </button>
    <% end %>
  </div>

  <div class="bg-gray-50 px-4 py-4 sm:px-6">
    <%= link_to "← Back to episodes", root_path, class: "text-indigo-600 hover:text-indigo-900" %>
  </div>
</div>